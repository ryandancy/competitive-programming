// 1194A - Remove a Progression
// The algorithm just removes all the odd numbers. The answer is 2*x.
#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
typedef long double ld;
int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    int T;
    cin >> T;
    while (T--) {
      int n, x;
      cin >> n >> x;
      cout << 2*x << endl;
    }
}
